\section{Problem Setup and Source-Mixture Certificate}

% Gate: write only after the narrative review accepts the source-mixture warrant.
%
% Required content:
% - Source domains, target domain, checkpoint bank, and source-loss vector.
% - Candidate-family approximation
%   L_T(\theta) \le \alpha_T^\top L(\theta)+\epsilon_{\mathrm{app}}.
% - Local tangent set around the uniform source mixture.
% - Robust value \bar L(\theta)+\rho\|P_\perp L(\theta)\|_2.
% - Soup endpoint vector z(w), deployed weights \bar\theta(w), and residual M(w).
%
% Proofs, derivations, and explanatory commentary stay in `appendix_proofs.tex`.
