\section{Limitations}

% Required content:
% - Source-domain labels are required.
% - The source-mixture approximation can fail when \epsilon_{\mathrm{app}} is large.
% - Weight-space deployment can fail when M(w) is large.
% - Replay and diagnostics add compute cost.
% - Claims are bounded by the checkpoint bank and protocol used.
